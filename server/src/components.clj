(ns components
  "Every separate module within the backend is organized as a component;
   every component consists of a list of dependencies to other components,
   and a lifecycle to create, start and stop the component." 
  )
